# `DataManager`

**파일 경로**: `src/managers/DataManager.js`

## 역할
아이템, 제작법 등 게임의 모든 정적 데이터를 관리하는 중앙 저장소.

## 주요 API
*   `getItem(id)`: 아이템 ID로 아이템 데이터를 반환합니다.
*   `getRecipes()`: 모든 제작 레시피 목록을 반환합니다.
*   `getRandomDrop()`: 확률에 따라 무작위 아이템 ID를 반환합니다.
